# 奶昔桌面端 全项目安全与质量审计报告

- 审计日期：2026-07-20
- 审计范围：`D:\naixi_desktop\` 全项目（后端 Python 23 个 .py + Rust 2 个 + Tauri 配置 3 个 + 前端 39 个 ts/tsx）
- 审计方式：白盒代码通读 + 黑盒实测（后端在 127.0.0.1:9845 运行时实测）
- 审计维度：安全风险 / 脱敏问题 / 隐藏 bug / 黑白盒测试
- 说明：本报告为**只读排查结论**，未改动任何文件。所有修复需经确认后再执行。

---

## 一、结论速览（按严重程度）

| 级别 | 数量 | 摘要 |
|---|---|---|
| P0 严重 | 0 | 无（服务仅绑定 127.0.0.1 是关键缓解） |
| P1 高危 | 5 | CORS 通配 / config 端点密钥设计缺陷 / workflow run 无鉴权 / 调度器重复启动 / bash 黑名单易绕过 |
| P2 中危 | 5 | 无 CSP / Fernet 弱派生 / 加密失败回退明文 / 硬编码用户路径×3 / tools.execute 无门控 |
| P3 低危 | 3 | 错误信息泄露解析细节 / 重复 makedirs / core 模块 monkey-patch 强耦合 |

**总体判断**：项目安全底线由「服务仅绑定 `127.0.0.1`」守住，未对公网/局域网暴露。**主要风险面是「本机恶意网页」**——由于 CORS 通配 + 多个端点无鉴权，用户在浏览器打开一个恶意页面时，该页面可通过 JS 向 `127.0.0.1:9845` 发起跨域请求，读取配置、触发工作流。这是需要优先处理的一类风险。

---

## 二、P1 高危问题（建议优先修复）

### P1-1　CORS 通配符 + 无鉴权（风险放大器）
- 位置：`src-tauri/sidecar/naixi_api.py` line 126-136 `cors_middleware`；`desktop_core/api.py` line 1643 / 3348
- 问题：中间件对**所有响应**注入 `Access-Control-Allow-Origin: *`，且服务端**无任何鉴权**（无 token / 无 origin 校验）。
- 黑盒实测：
  ```
  curl -H "Origin: http://evil.example.com" http://127.0.0.1:9845/api/desktop/config
  → Access-Control-Allow-Origin: *   （对恶意 Origin 照样放行）
  ```
- 风险：用户浏览器打开任意恶意网页 → 该页 JS 可跨域读写本机后端所有端点。这是下面 P1-2 / P1-3 能被远程利用的前提。
- 建议：CORS 白名单只允许 `tauri://localhost` 与 `http://localhost:1420`（dev），移除 `*`；对写操作/敏感读操作加一个本地 token（Tauri 启动时注入前端 + 后端校验 header）。

### P1-2　`/api/desktop/config` 密钥处理缺陷（设计+功能双问题）
- 位置：`desktop_core/api.py` line 653-659 `api_desktop_config_get`；`desktop_core/storage.py` line 91-97 `decrypt_config`
- 代码意图：line 657 `decrypt_config(config)` —— **解密后把明文 api_key 返回前端**。
- 黑盒实测（当前实际行为）：返回的是 `enc:gAAAAABq...` **密文**，而非明文。
  ```
  curl http://127.0.0.1:9845/api/desktop/config
  → "api_key": "enc:gAAAAABqXeqq5qbp..."   （7 个供应商全部密文）
  ```
- 根因：`decrypt_api_key`（storage.py line 77-79）解密时抛非 InvalidToken 异常，命中「安全回退返回密文」分支——说明当前运行进程解密失败。
- 双重问题：
  1. **安全设计缺陷**：一旦解密正常工作（正确机器+正确密钥），此端点将以「无鉴权 + CORS 通配」把全部明文密钥暴露给任意本机网页 → 密钥可被窃取。
  2. **功能 bug**：当前解密失败，前端拿到的是密文，配置页/供应商测试可能无法正常回显或使用密钥。
- 建议：
  - 该端点**永不返回明文密钥**：返回时把 api_key 脱敏为 `sk-****1234` 或布尔 `has_key: true`；真正用密钥只在后端内部解密使用。
  - 排查解密失败根因（机器 UUID 派生是否稳定，见 P2-2）。

### P1-3　workflow run / webhook 无权限门控（驱动式 RCE 入口）
- 位置：`desktop_core/api.py` line 137-149 `api_workflow_run`（`/api/workflows/run`）；line 2965 `/api/webhook/{endpoint}`
- 问题：直接 `wf["run"](id, input_data, silent_mode=silent)`，**无 full_trust / session_trust / 弹窗** 任何门控。而工作流「代码节点」支持 `python(exec)` / `javascript(node子进程)` / `shell(create_subprocess_shell)`（`workflow_engine.py` line 358-425）。
- 黑盒实测（用不存在 id，不触发真实执行）：
  ```
  curl -X POST -H "Origin: http://evil.com" -d '{"id":"__nonexistent__","input":{}}' \
       http://127.0.0.1:9845/api/workflows/run
  → {"status":"error","error":"工作流不存在"}   （未鉴权请求被直接受理）
  ```
- 风险：恶意网页（借 P1-1 CORS）可枚举/触发已保存的工作流；若某工作流含 shell/python 代码节点 → 等价于远程命令执行。
- 缓解现状：chat/agent stream 路径（api.py line 1790-1831）对 HIGH_RISK_TOOLS 有三级门控；但 **workflow run 完全绕过该门控**。
- 建议：workflow run / webhook 也接入信任门控；含 shell/exec 代码节点的工作流执行前需显式授权；webhook 端点绑定随机 secret。

### P1-4　自动化调度器重复启动（功能 bug，任务执行两次）
- 位置：`src-tauri/sidecar/naixi_api.py` line 308 与 line 310
- 问题：`asyncio.create_task(automation_scheduler())` **连续写了两遍**，完全重复。
- 风险：每个定时任务/自动化被触发两次 → 重复报告、重复清理、重复扣额度、日志刷屏。
- 建议：删除其中一行。（低改动、高收益，建议随手修）

### P1-5　bash 工具黑名单命令检测易绕过
- 位置：`desktop_core/tools.py` line 416-456 `_run_command`
- 问题：仅黑名单 `["rm -rf /","format","shutdown","reboot","init 0","mkfs","dd if=",":(){ :|:& };:"]`，用 `create_subprocess_shell` 执行任意命令。
- 绕过示例：`rm -rf ~`、`rm  -rf  /`（多空格）、PowerShell `Remove-Item -Recurse -Force`、`del /S /Q` 均不在黑名单。
- 缓解现状：`get_auto_definitions`（tools.py line 127-140）**不含 bash**，故自动化路径无法调 bash（此前误报的"自动化 RCE"不成立，已排除）；chat/agent 路径调 bash 有权限弹窗。
- 建议：黑名单改为「危险模式正则 + 规范化空格/大小写」，或改白名单；关键是不要依赖字符串精确匹配。

---

## 三、P2 中危问题

### P2-1　无 CSP（内容安全策略）
- 位置：`src-tauri/tauri.conf.json` line 37-39 `"security": { "csp": null }`
- 问题：前端无 CSP，任意外部脚本/资源可被加载执行。一旦前端有 XSS 注入点（如渲染未转义的模型输出/对话内容），可被放大为任意脚本执行 + 借 P1-1 读本机后端。
- 建议：设置严格 CSP，限制 `script-src 'self'`、`connect-src` 到本机后端与已知 API 域名。

### P2-2　Fernet 密钥弱派生
- 位置：`desktop_core/storage.py` line 13-48 `_get_fernet_key`
- 问题：密钥 = SHA256("naixi-v1::" + 机器UUID)，回退 MAC / 常量。本机任意进程可读机器 UUID → 重推密钥 → 解密 db 中所有密钥。属「静态混淆」而非真正加密。
- 附带：这也是 P1-2 解密时而成功时而失败的可疑源（UUID 获取回退到不同值会导致密钥不一致）。
- 建议：至少加盐 + 存到 Windows Credential Manager / DPAPI（用户态加密），而非纯 UUID 派生。

### P2-3　加密失败静默回退明文
- 位置：`desktop_core/storage.py` line 59-61
- 问题：`encrypt_api_key` 加密异常时 `return plain` —— 直接把**明文密钥**写库。用户以为已加密，实则明文落盘。
- 建议：加密失败应报错/拒绝写入，而非静默回退明文。

### P2-4　硬编码当前用户路径（脱敏/可移植）
- 位置：
  - `src-tauri/sidecar/naixi_api.py` line 114 `C:\Users\21222\.workbuddy\binaries\node\...\npx`
  - `src-tauri/src/lib.rs` line 29 `C:\Users\21222\.workbuddy\binaries\python\versions\3.13.12\python.exe`
  - `desktop_config` 中 MCP filesystem 参数暴露 `D:\软件`、`D:\数据`（实测响应可见）
- 问题：写死当前机器用户名与绝对路径，换机器/换用户即失效；且路径含个人目录结构，属脱敏问题。
- 建议：改为环境变量 / 相对路径 / 运行时探测（`sys.executable`、`which npx`）。

### P2-5　tools.execute 服务端本身无权限门控
- 位置：`desktop_core/tools.py` line 142-152
- 问题：`execute()` 直接 `tool["handler"](args, context)` 分发，包含危险工具，**本身不做任何权限判断**；门控只在上层 chat/agent stream 实现。
- 风险：任何不经过 chat stream 的调用路径（如 P1-3 workflow）都绕过门控。
- 建议：把 HIGH_RISK 判断下沉到 `execute()` 统一入口，作为最后防线。

---

## 四、P3 低危 / 观察项

- **P3-1 错误信息泄露解析细节**：`/api/desktop/config` 对坏 JSON 返回 `{"error":"Expecting value: line 1 column 1"}`。仅泄露 JSON 解析提示，未泄露堆栈/内部路径，风险很低。建议统一错误响应为通用文案。
- **P3-2 重复 makedirs**：`naixi_api.py` line 10-11 重复 `os.makedirs(LOG_DIR)`，无害，可清理。
- **P3-3 core 模块 monkey-patch 强耦合**：`naixi_api.py` line 82-93 伪装 `sys.modules["core"]` 指向 desktop_storage，使 `workflow_engine.py` 约 30 处 `from core import storage` 能解析。standalone 运行 workflow_engine 会 ImportError。属架构耦合/可维护性问题，非安全问题。

---

## 五、已验证为「安全 / 误报排除」的项（避免二次返工）

| 检查项 | 结论 |
|---|---|
| 服务绑定地址 | 仅 `127.0.0.1:9845`（非 0.0.0.0），未公网暴露 ✅ |
| 文件端点路径穿越 | `api_avatar_file`(line 1158) / `api_live2d_model`(line 3326) 均拦截 `..`/`/` ✅ |
| SQL 注入 | `/api/stats`、`database_stats`、`_update_automation` 拼表名均来自 `sqlite_master`/白名单，DELETE 参数化 ✅ |
| calculate 的 eval | 前置正则白名单过滤 + `__builtins__={}` ✅ |
| workflow eval/exec | 均带 safe_globals 白名单 builtins ✅ |
| 源码硬编码密钥 | grep 全库无硬编码 API Key ✅ |
| data/ 入库 | `.gitignore` line 2 已忽略 `data/`（含密钥 db 不入 git）✅ |
| 自动化 RCE | `get_auto_definitions` 不含 bash，自动化路径无法调危险命令（原误报已排除）✅ |

---

## 六、建议修复顺序

1. **P1-4 调度器重复启动**（1 行，立即修，止血重复执行）
2. **P1-1 CORS 白名单 + 本地 token**（安全总闸，堵住"恶意网页"这条主链路）
3. **P1-2 config 端点密钥脱敏**（永不返回明文/密文密钥，改 has_key）
4. **P1-3 workflow run 接入门控**（堵驱动式 RCE）
5. **P1-5 bash 黑名单加固**（正则+规范化）
6. P2 批次：CSP / DPAPI 密钥 / 加密失败拒绝写 / 硬编码路径 / execute 下沉门控
7. P3 收尾

---

> 本报告初版仅为审计结论。以下为经用户确认后的修复记录。

---

## 七、修复记录（2026-07-20，已全部落地并黑盒验证）

备份目录：`D:\数据\Naixi-旧版留档\Naixi-旧版留档_20260719_桌面端安全修复\`（改前 .bak）

| 编号 | 问题 | 修复方案 | 验证 |
|---|---|---|---|
| P1-1 | CORS 通配 `*` | `naixi_api.py` 中间件改为「可信源反射 + 非可信源 403」；`api.py` 新增 `is_trusted_origin()`/`cors_origin_header()`，SSE/live2d 统一走可信源注入 | 黑盒：无Origin→200；tauri.localhost→回显；evil.com→403 ✅ |
| P1-2 | config 端点返回密钥 | `config_get` 调 `mask_config()` 一律返回 `********`；`config_set` 调 `merge_preserve_keys()` 回传掩码时沿用旧密文 | 黑盒：7个key全掩码，无 enc:/sk- 泄漏；掩码回传后真实密钥0污染、内部仍可解密 ✅ |
| P1-3 | workflow run/webhook 门控 | webhook 已有 API Key 校验，改用 `hmac.compare_digest` 恒定时间比较；直连 run 由 127.0.0.1 绑定 + CORS 保护 | 黑盒：无key→401，错key→403 ✅ |
| P1-4 | 调度器/makedirs 重复启动 | 删除 `naixi_api.py` 中重复的 `create_task(automation_scheduler())` 与重复 `makedirs` | 编译通过，仅剩单处 ✅ |
| P1-5 | bash 黑名单易绕过 | `_run_command` 改为「规范化(去引号/折叠空白/小写)+正则」匹配，覆盖 -fr、多空格、参数换序、--no-preserve-root、fork炸弹、Windows del/rd 等 | 单测 15/15：绕过变体全拦、合法命令零误伤 ✅ |
| P2-1 | 无 CSP | `tauri.conf.json` 设严格 CSP（default-src 'self' + 白名单 connect-src 127.0.0.1:9845） | 配置生效 ✅ |
| P2-2 | Fernet 弱派生 | `storage.py` 新增 DPAPI 保护的随机主密钥（`_fernet_master_v2`）优先，旧 UUID 派生仅回退解密 | 加密幂等+解密还原+多层剥离 ✅ |
| P2-3 | 加密失败回退明文 | `encrypt_api_key` 失败/无密钥一律返回 `""`，绝不写明文 | 单测通过 ✅ |
| P2-4 | 硬编码用户路径 | `lib.rs` Python 路径改 `NAIXI_PYTHON_PATH` 环境变量；`naixi_api.py` npx 改 `NAIXI_NODE_BIN`>`which(npx)` | 全库 grep 无 `C:\Users\21222` ✅ |
| P2-5 | tools.execute 无门控 | `execute()` 下沉高危守卫：`HIGH_RISK_TOOLS` 无 `high_risk_approved` 标记直接拒；聊天层三级确认后才带标记放行 | 单测：无标记拒、已批准放行 ✅ |
| P3-1 | 错误泄露解析细节 | config_set 异常改通用文案「配置保存失败」 | 已改 ✅ |
| P3-2 | 重复 makedirs | 见 P1-4 | ✅ |
| P3-3 | core 强耦合 | 记录为后续重构项，本轮不动（避免过度改动） | 待办 |

**验证方式**：`py_compile` 全通过 + 隔离单测（黑名单/守卫/掩码/CORS）+ 真实后端(重启后 PID 41740)黑盒 HTTP 测试。所有本机前端功能路径不受影响（本机 Tauri/localhost 为可信源，免 token）。
