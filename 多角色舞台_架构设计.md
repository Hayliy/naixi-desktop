# 奶昔 · 多角色舞台（AI 共演）架构设计 v0.2

> 状态：设计稿（待评审，未实现）
> **唯一设计约束（用户原话，本稿一切以此为锚）**：
> **真人能做什么，要求就有什么；真人会发生什么，LLM 就应该有什么。**
> 即：所有 LLM 角色的能力与情境，必须 1:1 对齐"真人在舞台上会怎样"。不发明真人做不到的抽象能力，也不漏掉真人自然会有的反应。

---

## 0. 设计方法论：真人基准（唯一裁判）+ 诚实边界

不是"AI 该有什么功能"，而是"一个真人演员在舞台上会怎样，角色就怎样"。

- **正向对齐**：每加一项能力，必须能回答"真人在同场景会怎么做"——回答不出来就不做。
- **反向护栏**：拒绝"只有 AI 才有的超能力"。例如真人不会"同时读 8 个数据源并秒级汇总"，所以不做过度聚合；真人做不到的（如瞬间全知），LLM 也不该假装。
- **情境覆盖**：真人会经历的所有场面（无聊/兴奋/被cue/被打断/冷场/新朋友来），都必须有对应的 LLM 反应路径，不能只覆盖"用户说话"这一种。

下面两表（§1/§2）是**通用能力锚点**，后续所有支柱都只是这两表的落地。

> **⚠️ 诚实边界（v0.3 订正）**：所谓"真人基准"是我从训练数据采样得来的二手概括，**不是真去观察了直播生态**。真实虚拟主播分类极多（唱歌/杂谈/整活/一起看视频/一起打游戏/代肝/主机/手游/ASMR/配音…），每种的"感知-决策-行动"组合都不同。**本系统不声称一次覆盖所有类型**；正确目标是搭"按直播类型(genre)可插拔"的框架，先把你指定的类型做深（详见 §9）。上一版"覆盖真人所有情境"是说过头了。

---

## 1. 真人基准映射表（能力 → LLM 角色）

| # | 真人能做什么 | 真人表现 | LLM 角色对应（我们要做的） |
|---|---|---|---|
| C1 | **看**（观察场景） | 边看屏幕/游戏/视频/音乐边吐槽，看到精彩处欢呼 | **场景感知**：读取共享场景状态（在玩什么/听什么/发生什么），作为脑子输入 |
| C2 | **听**（听见彼此） | 面对面，别人说话我听得到 | **角色间广播**：任一角色发言都进共享上下文，其他人听得到 |
| C3 | **说/接话/插嘴/让话** | 听见有意思的就接，抢话时有人让 | **发言 + 选角仲裁**：判断是否接话、不抢话、轮到自己才说 |
| C4 | **有性格/梗/偏好** | 每人说话风格、口头禅、立场不同 | **独立人格 system prompt**（每角色一套） |
| C5 | **记得共同经历** | 老搭子有共同记忆、能接旧梗 | **每角色独立记忆**，不串台 |
| C6 | **表情肢体**（笑/哭/惊/怒/比划） | 说到激动就变脸、比手势 | **表情 + 动作驱动**（嘴型/表情/动作） |
| C7 | **动手**（打游戏/切歌/比划整活） | 真能操作物体、按键盘、比心 | **行动层 tool**：操控/特效/媒体控制 |
| C8 | **发呆/自言自语** | 没人时也晃悠、哼歌、碎碎念 | **idle / 自主闲聊**，不刷屏 |
| C9 | **多人同框但不乱** | 有人主导、不抢麦、轮流来 | **选角不抢话 + 轮次收束** |
| C10 | **临场反应** | 被点名立刻答、听到笑话就笑、被打断会停 | **触发式反应**（被cue/笑话/打断分别处理） |

## 2. 真人情境映射表（会发生什么 → LLM 要经历什么）

| # | 真人会发生什么 | LLM 角色对应 |
|---|---|---|
| S1 | **无聊**（半天没动静） | idle / 自聊破冰，但不刷屏 |
| S2 | **兴奋**（游戏赢了/名场面/副歌） | 场景触发**集体欢呼/吐槽**（你说的"对游戏视频音乐的交互"） |
| S3 | **被点名** | 精准回应那个被cue的人 |
| S4 | **别人讲笑话** | 笑 / 接梗 |
| S5 | **被打断** | 中断处理、把话让出去 |
| S6 | **多线程**（看剧同时聊天） | 多上下文感知，不混乱 |
| S7 | **新朋友加入** | 动态登场、互相介绍 |
| S8 | **冷场** | 有人救场 / 自聊破冰 |

> 这两张表就是验收标准：哪条没覆盖，就是"漏了真人的一部分"。

---

## 3. 四大支柱（每条标注"对应真人"）

### 3.1 独立脑子（Mind）——对应 C4/C5
真人：每个人有自己的脑子和记忆，不会共用一个大脑。
- 每角色持有：`llm{model,api_url,api_key}` + `persona` + `memory[]`。
- `_call_llm(prompt,*,agent)` 按 `agent.mind` 覆盖模型/人格/历史；不传则回退全局（naixi 默认全局，兼容"保留全局不动"）。
- 持久化：`live_connector_mind`（agent_id→{model,api_url,api_key(enc),persona}）、`live_agent_memory`（per-agent，压缩复用 `context.py`）。
- 远端 HTTP/WS 角色脑子在远端，这边只渲染+路由，其 LLM/人格字段**置灰标注"脑子在远端"**（对应：真人请来的外援自带脑子，你不能替他选）。

### 3.2 场景感知（Scene Bus）——对应 C1/S2/S6
真人：眼睛看着屏幕，知道现在在玩啥、听啥，据此吐槽。
- `scene_bus` 统一事件流：`{type,source,payload,ts}`，type∈{media_video,media_music,game_event,user_action,agent_speech}。
- Provider 可插拔：**起步先做手动/半自动注入**（用户点"正在玩XX/切到BGM"），后续接播放器 hook / 屏幕捕获作为插件叠加——不阻塞主线（对应：真人先看得到，再谈自动读屏）。
- 事件广播给 Director + 写入各角色 memory 作共享上下文。

### 3.3 舞台导演（Director / 互馈）——对应 C2/C3/C9/C10/S1~S8
真人：面对面，听见别人说就接；有人主导不抢话；无聊时自聊；兴奋时集体欢呼；被打断就让。
- 三类触发（覆盖情境表全部广度）：
  - ① **接话型**（C2/C3）：用户或某角色发言→其他人被问"要不要接"。
  - ② **自主闲聊型**（C8/S1）：idle timer，无人也按节奏自聊（living scene）。
  - ③ **场景触发型**（C1/S2）：游戏/视频/音乐变化→指定/集体角色反应（**你最看重的一块**）。
- 选角不抢话（C9）：相关性+轮次+冷却挑 1~2 人；正在说者让位；同事件不重复触发同一人。
- 临场反应分支（C10/S3~S8）：被cue精准答、笑话接梗、打断让话、新朋友登场介绍、冷场救场。
- 轮次收束：2~3 轮你来我往后达上限/无人想接/冷却即停，避免刷屏。
- 与现有 `SpeechArbiter`（已按 model_key 隔离）协同：Director 管"说不说/谁说"，Arbiter 管"同模型多角色不撞麦"。

### 3.4 行动层（Action）——对应 C7
真人：不只是动嘴，还能动手打游戏、切歌、比划、整活。
- `AgentOutput = {speak?, emotion?, action?, tool?{kind,...}}`。
- `tool.kind`：`game_control`（操控游戏）/ `media_control`（暂停/切歌/调音量）/ `fx`（特效音效）/ `expression`（复用现有 WS）。
- 后端 `action_executor` 分发到对应 Provider。实现"每个角色都能操控打游戏、吐槽、欢声笑语"——脑子决策→嘴说+手做。
- **权限边界（待拍板，见 §6）**：真实键鼠注入需授权有风险；舞台内模拟则纯表演安全。真人能真动手，但"让 AI 动你电脑"需你明确授权。

---

## 4. 现状盘点（已读代码核实，非猜测）

| 维度 | 现状 | 出处 | 距真人基准差在哪 |
|---|---|---|---|
| 本地 LLM 角色 | 仅 `naixi` | `live_bus.py:394` | 只有一个"真人"，其余是渲染木偶 |
| 人格/记忆 | 全局 `_live_prompt`+`_scene_history`，无 per-agent | `live_engine.py:1512/1521/1523` | 违反 C4/C5：所有人共用一个脑子 |
| 角色间互馈 | **零**，A 不触发 B | `_decide_reply` 仅 naixi 用 | 违反 C2/C3：面对面却各说各的 |
| 场景感知 | **无** | — | 违反 C1/S2：看不见屏幕 |
| 行动层 | 仅 speak+表情+动作，无 tool | WS 消息限定 | 违反 C7：只能动嘴不能动手 |
| 舞台渲染 | 仅渲染 sprite+路由发言 | `StageWindow.tsx` | 看不见谁在说/接/动手 |

**结论**：现在连"一个真人"都勉强（naixi 有脑子但看不见场景、不能动手）；其余角色是木偶。需新增四大支柱补齐到"多真人同台"。

---

## 5. 数据模型 & 接口（草案）

### 新增 meta 键（SQLite `meta`，沿用"SQLite 存数据"铁则）
- `live_connector_mind`：`{agent_id:{model,api_url,api_key(enc),persona}}`
- `live_agent_memory`：`{agent_id:[{role,content}]}`（per-agent，压缩复用 `context.py`）
- 沿用 `live_model_bindings`（渲染模型）、`live_backend_kinds`（渲染后端）。

### API（新增/扩展）
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/live/connectors` | 增返 `llm_model`/`persona`（key 脱敏） |
| POST | `/api/live/connector-mind` | 设某角色 LLM+人格，落 `live_connector_mind` |
| POST | `/api/live/connector-register-local` | 新建本地驱动角色（填 LLM+人格即本地思考） |
| POST | `/api/live/scene-event` | 注入场景事件（游戏/视频/音乐/用户），入 Scene Bus |
| WS | `/api/live/live2d-stream` | 现有；新增 `tool`/`scene`/`active_speaker` 消息类型 |

### WS 消息（扩展）
- 现有：`speak`/`audio`/`avatar_expression`/`avatar_motion`/`avatar_params`
- 新增：`scene`（场景条：正在玩X/音乐Y）、`tool`（行动可视化：谁在操控/触发了什么）、`active_speaker`（高亮当前发言角色）

---

## 6. 分阶段实施（每阶段对照真人基准验收）

| 阶段 | 内容 | 对应真人基准 | 可验点 |
|---|---|---|---|
| **P0 独立脑子** | 泛化 `LocalAgentConnector` + `_call_llm` per-agent 覆盖 + mind 存储/API + 前端角色卡（LLM+人格） | C4/C5 | naixi 与新建角色配不同模型/人格，对话明显不同；记忆不串台 |
| **P1 场景感知** | `scene_bus` + 手动/半自动场景注入 + 前端场景条 | C1/S2/S6 | 手动"正在玩XX/切音乐"→场景条显示，事件入各角色 memory |
| **P2 Director 互馈** | 选角+轮次+三类触发+临场反应分支 | C2/C3/C9/C10/S1~S8 | 用户说话他人接话；游戏/音乐变化集体反应；被cue精准答；不打断抢话；能收束 |
| **P3 行动层** | `AgentOutput.tool` + `action_executor` + 游戏桥/媒体控制/特效 | C7 | 角色能"动手"：控制播放、触发特效、模拟操控游戏 |
| **P4 舞台重建** | `StageWindow` 重做：多角色同框、发言高亮、场景条、行动可视化、active_speaker | 全部 | 直观看到谁在说、谁在接、当前场景、谁在动手 |

> 复用：渲染骨架+WS 路由、麦位仲裁 `SpeechArbiter`、上下文压缩 `context.py`、QQ 侧已有 tool 范式。

---

## 7. 待你拍板的开放问题（重新框定）

1. **场景捕获深度（C1）**：真人"看得到屏幕"要做到多真？
   - (a) 仅手动/半自动注入（起步快，P1 即玩）；
   - (b) 接本地播放器 hook（自动知在听啥）；
   - (c) 屏幕/游戏自动捕获（最完整但涉及权限、工程量最大）。
   建议先 (a)，(b)(c) 作后续插件。**绝不因"AI 能读全屏"就跳到(c)——真人也是先肉眼看。**
2. **行动层权限（C7）**：真人能真动手，但"让 AI 动你电脑"要你明确授权。真实键鼠注入（有风险）还是舞台内模拟（纯表演安全）？这决定 P3 安全设计。
3. **先出可玩 demo 节奏**：是否 P0+P1 先合"能配不同脑子+能注入场景"的最小可玩版，再迭代 P2/P3/P4？
4. **naixi 与聊天页全局模型**：按你"保留全局不动"，naixi 默认沿用全局 `bailian`，舞台可覆盖。确认无误。

---

## 8. 风险与防坑（沿用项目铁则）

- 改 `desktop_core/*.py`（mind/director/scene_bus）后必须 `python src-tauri/stage_core.py` 同步 resources + 手 cp `target/release`/`target/debug` + 清 `__pycache__`，三处 md5 一致；后端需**完全退出 Tauri 重开**生效。
- per-agent memory 必须隔离，杜绝"全局 `_scene_history` 串台"（当前最大坑，违反 C5）。
- API Key 走 `encrypt_config` 加密（沿用 `desktop_config`），前端脱敏返回。
- 不新建 JSON/YAML 存数据（项目铁则），全落 SQLite `meta`。
- 近端验证：沙箱直连真机 9845 WS 发 `scene-event`/`chat`，看 Director 是否产出多角色互馈+`tool`，而非只靠用户肉眼。

---

## 9. 一个真人就能都做 → 系统该是"一个能适应的脑子"，不是 genre 菜单（v0.4 订正）

### 9.1 用户点破的逻辑（正确）
用户："上述那些一个真人就能都做，你能怎么办？"——真人不是"加载不同模块"切换直播类型，而是**一个脑子 + 一对眼睛耳朵 + 一双手**，靠实时感知现场、临场判断，什么类型都能临时切换。我上一版把类型拆成"genre 包 = 独立行为模块"，是过度碎片化、且暗含"我做不到全部所以分开"的退缩。正确模型应更接近真人：**一个统一的自适应 Agent**，类型只是它根据"当前场景 + 用户意图"临时进入的**软模式(soft mode)**，不是硬代码边界。

### 9.2 真人相对 AI 的真正代差（不回避）
真人主播的核心超能力是三件实时的事，这恰是 AI 系统的真实短板：
1. **实时多模态感知**：眼睛看屏幕帧、耳朵听歌/听弹幕语气、身体感"场子热不热"。AI 没有原生眼睛耳朵，全靠**外部喂场景数据**——喂得越真，越像人；喂不到，连聊天都像瞎子。
2. **实时身体执行（动手）**：手打游戏、嘴唱歌、比手势。AI 的"手"= 工具/输入注入，"嘴"= TTS。这两样在实时性/表现力上远弱于人。
3. **低延迟临场 improv**：人类 <1s 带语气接话；LLM 往返 + TTS 有固有延迟，调不好就机械。

→ 所以"能不能做所有类型"的真正分界线不是"类型"，而是**这件事有多依赖实时感知 + 实时身体执行**。

### 9.3 能力包络（诚实，按"依赖感知/执行的程度"分）
同一 Agent，按激活的能力与场景源不同，自然呈现出不同直播形态。诚实标注我做到什么程度：

| 直播形态 | 依赖 | AI 做到的程度 | 差距来源 |
|---|---|---|---|
| 杂谈/聊天/连麦 | 语言+表情 | ✅ 强（最接近真人） | 延迟/语气机械感 |
| 整活/高互动 | 语言+fx+表情 | ✅ 强 | 同上 |
| 一起看视频/反应式吐槽 | 媒体流感知+语言 | ✅ 较强（**前提是喂到当前视频/音乐元数据**） | 感知喂得粗→反应空泛 |
| 一起打游戏（解说/陪聊） | 游戏状态感知+语言 | ⚠️ 中（能"说"游戏，难"玩"游戏） | 实时操作弱 |
| 代肝/实操打游戏 | 真实键鼠执行+决策 | ❌ 弱（慢、脆、需授权、难达人类水平） | 身体执行代差 |
| 唱歌/声乐 | 旋律化嗓音合成 | ❌ 弱（TTS 难真"唱"出旋律情感） | 发声器官代差 |

→ **强项全在"感知+语言+表情"这一侧**；弱项全在"实时身体执行/声乐"这一侧。一个真人都能做，是因为真人三件都齐；AI 目前只齐了一件半。

### 9.4 因此"你能怎么办"的诚实答案
- **架构**：做一个**统一自适应 Agent**（一个脑子 + 共享记忆 + 广播），类型 = 软模式（激活哪些场景源/能力/人格微调），**不是硬拆 genre 模块**。内核提供通用机制，软模式只调参数，不重写行为。
- **优先级**：先把"感知+语言+表情"强侧做透（杂谈/整活/反应式同看同玩陪聊）——这一大块我确实能逼近真人，且是多数直播的主形态。
- **弱侧诚实标注**：代肝/竞技实操/唱歌作为"辅助/模拟"能力提供，明确告知用户"这部分我不如真人"，不假装全能。
- **决胜变量 = 感知喂入**：给 Agent 真实场景流（屏幕/OCR/媒体元数据/游戏状态桥），强侧形态立刻变真；不给，强侧也塌。所以 P1 场景总线是重中之重，且要往"自动捕获"走（见 §7 开放问题①），不能只停手动注入。

> 实施修正：原 P0–P4 不变，但 P2 Director 改为"**一个自适应决策器 + 按软模式加载参数**"，而非"按 genre 包加载不同硬编码规则"。genre 作为软模式配置（scene 源 + 能力开关 + 人格），存 `live_genre_modes`，不动内核。

---

## 10. 反"AI 味"的核心：内在自我与能动性（v0.5 订正 · 最关键的纠偏）

### 10.1 用户点破（无法反驳）
用户："哪怕做出来也是一股 ai 味……人能做什么是非常复杂的一件事，乐意做什么不乐意做什么，喜怒哀乐各种思想等等。虚拟主播看着小，但皮套背后站着的就是真人。"
→ 此前的全部设计（能力包络/genre/场景总线）都还在"**把 VTuber 当功能交付系统**"的框架里——优化"能做什么"。但用户直觉到的"AI 味"根源不在这里：**一个只会响应触发、永远乐于助人、完全透明、每次开播像重置的角色，本质上没有"自我"，所以不像人。**

### 10.2 "AI 味"到底从哪来（对照真人逐项）
| 真人主播 | 当前/典型 AI 做法 | 所以闻起来像 AI |
|---|---|---|
| 有**意愿**：乐意/不乐意，会拒绝、会敷衍 | 纯响应式，来啥接啥，永远配合 | 没有"不想"这件事 |
| 有**心境**：自己漂移，不靠事件触发 | 情绪=对事件 X 的反应函数 | 没有"无缘无故的心情" |
| 有**连续性自我**：跨场次累积、会变 | 记忆=可检索聊天日志，开播如初 | 没有"昨天不爽今天还记着" |
| 有**私心想法的间隙**：说的是一套，心里另一套 | 内心=输出，全透明 | 人不全透明，AI 全透明 |
| 有**自己的议程**：偶尔不理你、自顾自做啥 | 纯被动，无人说话就空 | 没有"它自己想干啥" |
| 有**偏见与口味**：明确好恶、有立场 | 中性、 helpful、无立场 | 没性格内核 |

**结论**："AI 味"≈ 缺自我（agency）+ 缺内在生命（inner life）+ 全透明。多堆能力/场景源治不了这个——甚至堆越多越像"功能演示"。

### 10.3 诚实边界（不假装能造人）
我**造不出真人**，只能**更好地模拟内在生命**。可信度提升的唯一杠杆，是把上表那些"缺失维度"建模为**一等公民状态**，而不是堆功能：
- `willingness`（意愿/边界）：什么乐意、什么抗拒、抗拒时怎么敷衍。
- `mood`（心境）：自有基线 + 缓慢漂移 + 事件扰动，非纯触发。
- `self_continuity`（连续性自我）：跨会话持久、会演化的内层状态（不是聊天日志）。
- `private_thought`（私密想法）：内部独白层，未必外显；"说的"与"想的"可不一致。
- `own_agenda`（自有议程）：无人触发时也会发起行为（发呆自语/去做自己的事）。
- `bias`（偏见口味）：稳定的好恶与立场，构成性格内核。

### 10.4 设计重心转移（最重要的一句）
**从"能力覆盖"转移到"自我建模"**。场景总线/能力/genre 仍是"这个自我能做什么"，但**中枢不是功能调度器，而是一个有意愿/心境/连续自我的 Agent**——它"选择"是否、如何行使能力，而非被事件驱动机械触发。Director 的新含义：不是"谁该接话"的调度员，而是"这个自我此刻想不想理、想怎么理"的决策器。

### 10.5 不可约核心：皮套背后站着真人 → 两条路分叉（待你定）
"皮套背后站着真人"这句话点出一个**不可约的事实**：彻底自主的 AI 永远缺一个真实自我，所以"完全取代人类"这条路天然带味。两条产品路线：
- **路线 A · 自主角色（模拟自我）**：接受"仍是模拟"，把 §10.3 维度做厚，做到"像一个有人格的角色/ companion"，不宣称取代真人。多数观众能接受"这是个角色"。
- **路线 B · 真人增强（human-in-the-loop）**：承认背后该是真人——AI 不是替身，而是**真人的大脑外挂/分身助手**：帮你续梗、替你记老粉、在你不在时维持皮套轻微活跃、把你的意图快速变成表演。皮套背后还是你，AI 是让你的"人"更省力更持久的放大器。
- 这俩不互斥，但**重心不同**：A 追求"像人"，B 追求"帮你更像是人"。用户原话倾向 B（强调背后是真人）——值得明确选。

### 10.6 实施修正
- P0 地基除"独立脑子"外，**新增"自我内核(self core)"**：willingness/mood/self_continuity/private_thought/own_agenda/bias 作为一等状态，持久化、跨会话演化。
- `_call_llm` 的系统层不止 persona，而是"当前自我状态摘要"（含心境/意愿/最近私密想法），让输出带"人味"而非中性回答。
- Director 决策改为"自我是否/如何响应"，默认带 willingness 与 mood 门控（不乐意就弱化/拒绝/敷衍）。

---

## 11. LLM 在直播的真实定位与赛道（v0.6 · 落地前的"搞明白"步骤）

### 11.1 先校准：LLM 是"语言互动层"，不是"全能主播"
LLM 本质=基于文本的理解+生成+推理。在直播里它真正能扛的是"说话/反应/记忆/人设"这一层；扛不了"动手/发声/实时视觉"。任何设计先锁死这条边界，才不会又飘成"全能"。

### 11.2 LLM 在直播能做的（强）
- **实时弹幕/连麦对话**：读聊天、自然接话、维持 flow（核心强项）
- **反应式解说/吐槽**：喂入场景文本（游戏状态/视频片段/音乐信息）→ 产出吐槽/解说（强，前提：场景被文本化喂入）
- **人设与语气一致性**：跨整场维持角色嗓音/口头禅/立场
- **氛围调动**：读场子情绪，捧场/安慰/开玩笑/带节奏
- **老粉记忆与关系**：记住常客、梗、连续互动（memory）
- **内容辅助（离线/半实时）**：写台词/标题/整活点子/剪视频文案
- **多语翻译**：实时翻弹幕
- **轻陪伴**：无人时皮套轻微活跃（需自我建模避免"空"）

### 11.3 LLM 在直播做不了 / 不该抢的（弱，排除出 LLM 赛道）
- **代肝 / 实操打游戏**：需真实键鼠 + 游戏感 + 毫秒反应 → 非 LLM 活，弱且有授权风险
- **唱歌 / 声乐**：TTS 难旋律化表达 → 弱
- **实时竞技 play-by-play**：延迟过高跟不上快节奏
- **实时视觉创作**：属扩散模型，非 LLM

### 11.4 赛道适配表
| 赛道 | LLM 是否正选引擎 | 说明 |
|---|---|---|
| 聊天 / 杂谈主播 | ✅ 正选 | LLM 即对话引擎 |
| 反应吐槽（看视频/看游戏） | ✅ 正选 | 需场景文本喂入 |
| 虚拟陪伴 / 24h 轻互动 | ✅ 适合（路线 A） | 自我建模避免空 |
| 真人内容辅助（写梗/剪文案） | ✅ 最佳（路线 B） | 纯增强，无取代问题 |
| 游戏陪玩解说（不操作） | ⚠️ 半 | 能说游戏、不能玩 |
| 代肝 / 实操 | ❌ 非 LLM 赛道 | 排除 |
| 唱歌 | ❌ 非 LLM 赛道 | 排除 |

### 11.5 落到首赛道建议
- **最强且最不像 AI 的切入 = 路线 B**：给真人当"对话 + 反应 + 老粉记忆 + 内容辅助"外挂（皮套背后还是你，AI 让你更省力更持久）。这恰是你强调"背后站着真人"的落地形态。
- 可选叠加路线 A 的"轻陪伴"，做你不在时皮套的轻微活跃（注意仍需自我内核，否则空）。
- **明确不做**：代肝 / 唱歌 / 实操不作为 LLM 赛道。
- 这步"搞明白"之后，P0/P1 才按此赛道落地：统一脑子（对话引擎）+ 场景文本喂入（反应吐槽）+ 老粉记忆 + 自我内核的意愿/心境——而非泛做全功能。
