# 安全策略 / Security Policy

## 支持范围

安全修复优先进入最新发布版。

| 版本 | 支持状态 |
| --- | --- |
| 1.0.x | 支持安全修复 |
| 0.2.x 及更早 | 不再支持（且卸载行为会删除用户数据，建议升级） |

## 如何上报漏洞

**请不要用公开 Issue 披露未修复的安全问题。**

优先走 GitHub 的私密上报通道：

1. 打开仓库 **Security** 标签页 → **Report a vulnerability**（GitHub Security Advisory，私密）；
2. 或发邮件至 `2122235245@qq.com`，标题以 `[SECURITY]` 开头。

请尽量附上：受影响版本、复现步骤、影响面判断（能读到什么 / 能改什么），以及你验证用的样本或日志片段。若涉及安装包伪造、签名绕过一类问题，请带上文件哈希。

**响应预期**：这是个人维护的项目，尽力在 7 天内给出首次回复；确认后会在修复版本发布时公开致谢（除非你要求匿名）。

## 安全模型与边界

理解边界比理解功能更重要。以下都是**已知并有意的设计**，不属于漏洞：

- **后端只监听 `127.0.0.1`**，不对外暴露端口，也不做远程管理。任何需要把后端暴露到局域网的改动都不在计划内。
- **不采集遥测、不上传使用数据。** 数据流向逐条列在 [docs/PRIVACY.md](docs/PRIVACY.md)。
- **API Key 以 Fernet 加密落盘**，密钥由本机标识派生。请自行保管操作系统账户安全——能读你用户目录的攻击者本来就能拿到一切。
- **游戏 Agent 只做截屏输入 + 键鼠输出**，不读游戏内存、不连服务器。但它注入的键鼠事件在联网竞技游戏中可能被判定为外挂，这是使用场景风险，不是程序漏洞。
- **安全中心（银狐应急防护）只处理用户态痕迹。** 银狐等木马使用 BYOVD 加载内核级 rootkit，**任何用户态程序都无法清除**；本项目不做能力误导，也**绝不提供对 C2 的反制能力**（对 C2 发起 DoS 或未授权访问既违法也无效）。
- **安装包是自签名证书。** 自签名不消除 SmartScreen「未知发布者」提示，它提供的是完整性与签名主体可核验。因此「SmartScreen 报未知发布者」属预期行为，不是漏洞；也请务必按 README「安全与完整性」完成哈希校验。

## 我们关心的漏洞类型

- 本地权限提升：后端或桌宠进程可被非本机用户/低权限进程利用。
- 路径穿越、任意文件读写或任意命令执行（尤其是能通过 HTTP 端点、工作流节点或工具调用触达的）。
- 密钥泄漏：能把 Fernet 加密的 API Key 解出明文或泄漏到日志/网络。
- 前端注入：能通过对话内容、导入的知识/工作流/模板触发脚本执行。
- 发布链路：能伪造安装包、绕过哈希校验或篡改更新源。
- MCP / 插件加载：能借配置实现未授权代码执行。

## 收到上报后的处理

确认后按「评估影响 → 修复 → 真机验证 → 发布 → 公开说明」推进。修复会带上可验证的证据（复现步骤前后对比），不会以「已修复」一句带过。
