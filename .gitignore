# 数据库（含用户创建的工作流、模板等数据）
data/

# Node
node_modules/
dist/
*.tsbuildinfo

# Rust / Tauri
src-tauri/target/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.local

# Python 缓存
__pycache__/
*.pyc

# 内置 SearXNG（154MB，打包时附加下载）
searxng/

# 工作区文件（用户创建的临时文件）
workspace/
__pycache__/
logs/
_motions/
logs/
pet_debug.log
pet_init.log

# 打包生成的资源（自包含 Python 运行时 + 暂存的纯代码桌面核心，体积大不入库）
src-tauri/resources/

# Godot 导出缓存（纯生成物，含空值导出凭据）
godot_renderer/.godot/
# Godot 导出产物与模型资源（体积大，不入库）
godot_renderer/export/
godot_renderer/models/
# Godot 场景引用的 3D 模型本体（单文件超 GitHub 100MB 上限，不入库，需自备）
godot_renderer/scenes/*.vrm

# 通用 Rust 构建目录（防子目录遗漏）
/target/

# 构建/运行日志与临时产物（不入库，可能含本地路径）
*.log
tauri_err.txt
tauri_out.txt
build_log_*.txt
verify_shots/
install_gui.log
verify2.log

# 下载产物 / 二进制工具（不进版本库，本地保留可从网重下）
VoicemeeterSetup_v2122.zip
# 本地 TTS 模型缓存（kokoro-onnx，~120MB，首次使用自动下载，不入库）
naixi_tts_models/
aria2.zip
aria2_tmp/
aria2c.exe

# ============ 不公开：隐私 / 大体积 / 第三方版权 / 内部文档 ============

# AI 会话记忆与工作区隐私（含个人偏好、项目决策，绝不入库）
.workbuddy/

# Minecraft 客户端+服务端整包（1.2G，含第三方游戏版权文件，不入库）
mc_test/

# 扫雷 Agent 运行时诊断截图与缓存（只保留源码入库）
minesweeper_agent/_live_shots/
minesweeper_agent/__pycache__/
minesweeper_agent/**/*.png
minesweeper_agent/_*.py

# 面捕测试样本图（可能含真人，不公开）
desktop_core/vrm_html/face_test_sample.jpg

# Godot 场景根目录 VRM 模型本体（单文件 350MB+ 超 GitHub 上限，且涉游戏 IP，不入库）
godot_renderer/*.vrm

# 本地审计 / 架构设计 / 安全报告（内部文档，不公开）
代码路径审计_收尾_20260804.md
多角色舞台_架构设计.md
安全审计报告_20260720.md
工程规范红线.md

# 本地审计/设计文档（移入忽略目录，不公开）
_local_docs/

# 本地验证 / 诊断 / 安装验证脚手架（引用本机绝对路径 D:\naixi_desktop，非产品运行所需，不公开）
install_gui.py
install_probe.py
verify_uninstall_gui.py
test_grounding_http.py
_e2e_quota_persist.py
src-tauri/visual_check.py
src-tauri/verify_*.py
src-tauri/diag_*.py
src-tauri/measure_timer.py
src-tauri/gen_top.py
src-tauri/installer/generate_banner.py
src-tauri/make_uninstall_ico.py
desktop_core/vrm_html/pose_analyze.py
desktop_core/vrm_html/pixel_band_analyze.py
minesweeper_agent/make_replay.py

# 截图 / 验证 / 调试脚手架（引用本机绝对路径，非产品运行所需，不公开）
src-tauri/shot.py
src-tauri/shot_pages.py
src-tauri/shot_real_installer.py
src-tauri/shot_testflow.py
src-tauri/shot_welcome.ps1
src-tauri/quick_shot.py
src-tauri/quick_shot2.py
src-tauri/quick_shot3.py
src-tauri/render_check.py
src-tauri/sample_bmp_test.py
src-tauri/p2_check.py
src-tauri/p4_check.py
src-tauri/installer_rendered.nsi
src-tauri/installer/test_checkbox.nsi
src-tauri/installer/test_checkbox2.nsi
src-tauri/installer/test_checkbox3.nsi
src-tauri/installer/test_label_width.nsi
src-tauri/installer/uninstall_sim.nsi
src-tauri/syntax_check.nsi
src-tauri/test_flow.nsi
src-tauri/test_flow_current.nsi
src-tauri/test_flow_old_working.nsi

