# 奶昔 0.2.0

> ⚠️ **尚未做代码签名**：本版安装包未用 OV/EV 代码签名证书签名，Windows SmartScreen 会提示「未知发布者」。这是已知预期，不是被篡改。请务必下载后先验哈希再安装。

## 安全中心（本次重点）

「设置 → 安全」tab 现已整合完整安全中心：

- **银狐应急哨兵**：检测 Defender 实时防护被关 / 被加整盘排除项、可疑计划任务、异常外连（C2 网段）、启动项——正是银狐类木马的致盲与驻留手法。
- **360 急救箱官方调用**：一键拉起 360 系统急救箱（已校验官方下载地址）。
- **一键急救**：对银狐**用户态可见痕迹**做应急清理（不含内核层 rootkit，UI 已注明）。
- **自动监测哨兵**：常驻监测上述异常并提示。
- **安装包完整性 · 本程序哈希**：展示主程序 `naixi-desktop.exe` 的 SHA-256，与官方 `sha256sums.txt` 的 `[主程序]` 段人工比对，识别伪造/替换。**程序内不内置清单做自动比对**（攻击者连清单一起换，自动比对无意义）。

## 校验方式（两步）

1. **下载后**：`sha256sum -c --ignore-missing sha256sums.txt`（清单与安装包放同目录）。
2. **安装后**：设置 → 安全 → 安装包完整性，复制页面哈希与清单 `[主程序]` 段比对。

## 其他

- 全项目 TypeScript 错误清零（Tauri v2 `setIgnoreCursorEvents` 更正 + `isTauri` TDZ 隐患消除）。
- dev 构建修复 Vite 误扫 `vrm_html` importmap 裸模块的阻断性报错。
- 新增 `docs/VM_SANDBOX_HARDENING.md`：虚拟机防逃逸加固清单（演示银狐对抗前必读）。

## 已知边界

- 银狐的内核层 rootkit 任何用户态程序（含奶昔）都干不掉，需专业杀软 + 安全模式全盘查杀。
- 奶昔不内置任何反制 C2 的能力（对 C2 发起未授权访问违法且无效）。
- 虚拟化隔离非 100%，真机演示前请先按文档做宿主机前置自检。
